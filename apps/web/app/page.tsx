/**
 * Temporary placeholder. The real home view lands in a follow-up commit
 * that wires the dashboard templates and organisms.
 */
export default function HomePage() {
  return (
    <main className="min-h-dvh grid place-items-center bg-canvas px-6">
      <div className="text-center">
        <h1 className="text-3xl font-semibold text-text-primary">Sextant</h1>
        <p className="mt-2 text-sm text-text-secondary">
          Setup in progress — dashboard lands next.
        </p>
      </div>
    </main>
  );
}
